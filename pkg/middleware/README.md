# pkg/middleware

HTTP middleware for the module system. Provides composable handler decorators.

Adapted from `~/code/herald/pkg/middleware/` (CORS + Logger only, no auth).

## Files (Phase 1)

- `middleware.go` — `System` interface, CORS middleware, Logger middleware
